// // 초기 상태값(state)
// const initialState = {
//   number: 0,
// };

// // 리듀서 : 'state'의 변화를 일으키는 함수
// // (state를 action의 type에 따라 변경하는 함수)

// // input(리듀서의 매개변수) : state, action
// // action : state의 상태를 어떻게 변경할지를 표현함 (객체임 / type, value를 가짐)

// const counter = (state = initialState, action) => {
//   switch (action.type) {
//     // action의 type에 따라서 작업을 수행할 것
//     default:
//       return state;
//   }
// };

// export default counter;
